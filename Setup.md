# Tooling:
## Java 21 JDK
* Make sure the folder with java.exe is on your system PATH
## Maven
## MySQL 8.x.x - Install server(no GUI, recommend SQLTools) or full(includes MySQL Workbench GUI)
## VScode: 
* Extension pack for java
* Spring Boot Extension pack
* Recommended: SQLTools, SQLTools MySQL (driver)